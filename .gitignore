# env — .env.example만 commit한다 (ARCH-001 §10)
.env.local
.env.prod
.env

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# node (frontend/ 내부는 frontend/.gitignore가 관리)
node_modules/

# misc
.DS_Store
backend/app/seeds/data/mediness_users.json

# 로컬 ai_worker 실행용 복사본 — SoT는 레포 루트 context/ (claude 샌드박스가 외부 symlink 차단)
ai_worker/workspace/context/
