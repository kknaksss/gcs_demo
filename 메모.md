# 다음 세션 인수인계 메모 — 실시간 웹훅 + 홈서버 배포

> 2026-07-09 세션 종료 시점 기록. MVP(수집→AI 분류→승인→탐색)는 로컬 실검증까지 완료.
> 다음 세션은 이 메모를 읽고 시작한다.

## 현재 상태

- WORK-001~006 구현 + 실검증 완료 — 실 Drive 45건 수집, 로컬 claude 분류 45/45, 승인→탐색 E2E
- 코드: `gcs_demo` main 2커밋 push됨 (`a6ac641` 전체 구현, `861a20d` 실검증 이슈 수정)
- 지금은 웹훅 없이 **60초 폴링** 운용. 웹훅 코드는 전부 구현·테스트 완료
  (`POST /webhooks/google-drive`, HMAC 검증, watch 등록/갱신) — 공개 HTTPS URL만 없음
- 로컬 실행 절차는 `gcs_demo/README.md` "로컬 실행 절차" 섹션이 SoT
- frontmatter id는 `CFO-` prefix (전역 유일 컨벤션)

## 다음 태스크: ① Drive 실시간 웹훅 ② 홈서버 배포 (doc-first)

1. **문서 먼저**: `40-architecture/deploy/` 신설(서버/컨테이너/NPM 프록시/도메인/env 주입 구조)
   + `30-work/` WORK-007(웹훅+배포, phase 분해). product-doc-pipeline 규칙 준수
   (index/log 갱신 + `python3 .agent/scripts/product_doc_pipeline.py` 검증)
2. **배포 결정 필요** (문서에서 확정):
   - 서버에서 ai_worker(claude) 실행 방식 — (a) linux claude-tools 마운트(ax-graph 방식,
     `~/.cache/axkg-live/.claude-tools` 참고, docker-compose.prod.yml 주석) vs
     (b) AI 분류만 맥에서 하이브리드 운용
   - 서브도메인 2개: 앱용 + 웹훅용(`GOOGLE_DRIVE_WEBHOOK_URL`) — `*.kknaks.cloud` 체계
3. **구현/배포**: `.env.prod` 구성(secret 평문 commit 금지) → compose prod 기동 →
   NPM 프록시 → webhook URL 등록 → admin 화면 `watch 갱신` → 실시간 수집 검증
   (파일 투입 후 수 초 내 반영, 폴링 fallback 유지)
4. 완료 시 work 문서 phase/완료 증거, log.md, Status Board 동기

## 주의

- `gcs_demo`는 **PUBLIC 레포** — 직원 실데이터 seed(`mediness_users.json`)는 gitignore 유지,
  secret은 어떤 파일에도 commit 금지
- AI job 소비는 **의도적 순차 1건 처리** (ARCH-001 확정) — 병렬화하지 말 것
- ai_worker workspace 자산은 **symlink 금지**(claude 샌드박스) — 복사 방식
- 배포 대상: 홈서버 docker + Nginx Proxy Manager (기존 `profile-api.kknaks.cloud` 패턴)
